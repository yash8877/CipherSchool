class Solution
  {
    public static boolean areRitations(String s1,String s2)
    {
      // if(s1.length()!=s2.length()) return false;
      // int len=s1.length();
      // for(int i=0;i<n;i++)
      //   {
      //     String rotatestr= s1.subString(1)+s1.subString(0,1);
      //     if(s2.equals(rotatedStr)) return true;
      //   }
      if(s1.length)!=s2.length()) return false;
      string str =s1+s1;
      return str.contains(s2);
      //return false;
      
    }
  }
